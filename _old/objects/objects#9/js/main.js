'use strict';

{
  // 配列

  const a = [1, 5, 10];

  // add
  // a.unshift(100);   // 先頭に追加
  // a.push(200, 300); // 末尾に追加（よく使う）

  // console.log(a);

  // remove
  a.shift();  // 先頭から削除
  a.pop();    // 末尾から削除
  console.log(a);
}
